import reactDOM  from "react-dom";
import styled from "styled-components";
import App from "./App";

function Zaprecall () {
    return (
        <App />
    )
}

reactDOM.render(Zaprecall(), document.querySelector(".root"))